# AGENTS.md

このリポジトリは、技術同人誌「自作 PC で始めるローカル LLM」（副題「Windows + Radeon を中心とした検証環境の構築」）の執筆・組版プロジェクトです。

## 執筆の基本方針

- 作業前に `README.md`、`book/OUTLINE.md`、既存原稿、関連する設定ファイルを読む。
- 原稿は `book/manuscripts/articles/` に配置し、掲載順は `book/manuscripts/config/articles.yml` で明示する。
- 各章は単独のブログ記事の寄せ集めにせず、前後の章とのつながり、重複、用語を調整して一冊として読めるようにする。
- 一次情報と実測を優先する。変化の速いモデル名、ソフトウェアのバージョン、対応 GPU、手順には検証時点を明記する。
- 事実、実測、推測を区別する。未検証の内容は断定せず、「推測」「未検証」などと記す。
- 外部記事やリポジトリを参照した内容には脚注または URL を付ける。既存の著者記事を再編集する場合も、巻末の出典一覧と対応づける。
- コマンドは原則として再現可能な形で掲載する。破壊的な操作、セキュリティ機能の無効化、大容量ダウンロードには影響と代替案を併記する。
- Windows と PowerShell の例を基本とし、CUDA、ROCm、Vulkan の違いを曖昧にしない。
- 製品名と技術用語の表記を既存原稿に合わせる。本文では「ローカル LLM」「マルチ GPU」「Radeon GPU」「ComfyUI」「llama.cpp」「ROCm」を使う。

## 原稿の形式

- front matter に `class`、`title`、`author`、`profile` を記述する。
- 本文の H1 は front matter の `title` と一致させる。
- 見出しレベルを飛ばさない。表には列の意味が分かる見出しを付け、コードブロックには言語を指定する。
- 画像を追加するときは章ごとの画像ディレクトリに置き、代替テキストを具体的に書く。出典元の画像を利用する場合は、利用権と出典を確認する。
- URL は可能な限り脚注にまとめる。紙面だけでも要点が理解できるようにし、リンク先の内容を前提にしすぎない。
- 章末に「まとめ」を置き、次章へつながる一文を添える。

## 転載記事の追加方針

ユーザーが既存記事の転載を指定した場合は、通常の再編集方針よりも以下を優先する。

- 記事は初出日の時系列順で収録する。本文は原則として元記事を転記し、必要な加筆修正だけを行う。章同士をつなぐための再構成や、書籍制作上の内情を本文へ追記しない。
- 各章の冒頭は front matter、`doc-header`、front matter と同じ H1 の順にする。
- 初出情報は `<aside class="publication-note">` で表示する。ラベルは `Information` とし、「これは YYYY 年 M 月 D 日に媒体名で掲載しました。適宜、加筆修正しています。」と掲載元の完全な URL だけを記載する。はてなブログの媒体名は「ブログ」とする。
- Web の埋め込み URL は `<div class="link-card">` に置き換え、サムネイル、タイトル、完全な URL を表示する。リンク先を誤認しないようドメインだけの行は設けず、紙面から参照できる完全な URL を省略しない。
- X のポストは `<div class="link-card x-post-card">` を使用し、投稿者名、アカウント名、投稿本文、添付画像または動画サムネイル、ポストの完全な URL を表示する。メディアがないポストは画像を省略する。
- 記事中の画像とカード用サムネイルは外部参照にせずダウンロードする。保存先は Markdown ファイルと同名のディレクトリ（例：`01_example.md` に対する `01_example/`）とする。
- PDF 組版の再現性を優先し、写真や動画サムネイルは JPEG、文字を含む図版や画面キャプチャは PNG で保存する。WebP のまま収録しない。
- 本文の表と画像本体は Markdown 記法を維持する。Markdown 画像から組版時に生成される `<figcaption>` を画像タイトルとして使い、原稿側で重複するキャプションを追加しない。表タイトルは Markdown 表を `<figure>` で囲み、`<figcaption>` を使用する。
- 本文の段落に `<p>` タグを使わない。HTML は `doc-header`、掲載情報、リンクカード、X カード、`figure` / `figcaption` など組版に必要な構造へ限定する。
- カードや掲載情報の見た目は `book/theme/theme-custom/themes/styles.css` の共通 CSS で調整し、各原稿へインラインスタイルを書かない。
- 追加後は画像ファイルの実体、HTML タグの開閉、完全 URL の表示を確認する。PDF では掲載情報、カード、画像、`figcaption`、表、改ページを目視確認する。

## 編集・検証フロー

1. `book/OUTLINE.md` で章の目的、参照元、重複範囲を確認する。
2. 原稿と `book/manuscripts/config/articles.yml` を更新する。
3. `yarn lint` を実行し、警告を確認する。自動修正だけで意味を変えない。
4. `yarn generate` で目次・著者紹介・奥付を生成し、掲載順とタイトルを確認する。
5. `yarn build` で電子版 PDF を生成する。原稿や CSS を大きく変更した場合は PDF の改ページ、表、コードブロック、脚注も目視確認する。
6. `yarn test` と `yarn check` も実行し、スクリプトや設定の回帰がないことを確認する。

## Git と Pull Request

- 原稿編集は `codex/` など内容が分かる作業ブランチで行い、`main` へ直接コミットしない。
- コミットは「構成と設定」「章の追加」「校正・組版」のようにレビュー可能な粒度へ分ける。
- コミット前に差分を読み、生成物、モデル、依存物、秘密情報を含めない。
- Pull Request には、章構成、主な追記、参照元、検証コマンド、残っている未検証事項を記載する。
- GitHub Actions が生成した PDF も確認し、ローカルと CI の差がないことを確かめる。
